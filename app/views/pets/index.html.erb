<h1>ペット一覧</h1>

<div>
<% @pets.each do |pet| %>
  <p><%= link_to image_tag(pet.image.url), pet_path(pet.id) %></p>
  <p class="pet_feature"><%= link_to pet.feature, pet_path(pet.id) %></p>
  <p class="pet_created_at"><%= link_to pet.created_at, pet_path(pet.id) %></p>
<% end %>
</div>
