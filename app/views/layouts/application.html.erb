<!DOCTYPE html>
<html>
  <head>
    <title>Lostbird</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1><%= link_to "lostbird", pets_path %></h1>
    <% if user_signed_in? %>
    <h2><%= link_to current_user.nickname, user_path(current_user.id) %></h2>
    <h2><%= link_to "メッセージ一覧", rooms_path %></h2>
    <% else %>
      <%= link_to "ユーザーを登録する", new_user_registration_path %>
      <br>
      <%= link_to "ログインする", new_user_session_path %>
    <% end %>

    <div class="container">
      <div class="row">
        <%= yield %>
      </div>
    </div>

  </body>
</html>
